version: "3.0"

services:
  server:
    container_name: main_bot
    build:
      context: .
    command: tail -F anything
    ports:
      - 1234:1234
  server-2:
    container_name: other_bot
    build:
      context: .
    command: tail -F anything
    ports:
      - 1234:1234
